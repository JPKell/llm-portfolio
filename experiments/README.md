# Experiments

Each observed experiment gets an immutable directory containing configuration, manifests, environment, lineage, raw-evidence index, selection/deviation record, and checksums. Large model/data files stay outside Git and are referenced by immutable public IDs or local hashes.

No learner model experiment is published in the initial scaffold.
